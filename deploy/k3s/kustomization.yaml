apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: shellguard

resources:
  - namespace.yaml
  - configmap.yaml
  - secret.yaml
  - pvc.yaml
  - postgres.yaml
  - shellguard.yaml
  - service.yaml
  - ingress.yaml

commonLabels:
  app.kubernetes.io/part-of: shellguard
  app.kubernetes.io/managed-by: kustomize
