apiVersion: v1
kind: Service
metadata:
  name: oto
  namespace: oto
  labels:
    app.kubernetes.io/name: oto
    app.kubernetes.io/component: server
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: oto
    app.kubernetes.io/component: server
